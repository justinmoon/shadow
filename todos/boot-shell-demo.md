# Boot Shell Demo

Living plan. Revise it as we learn. Do not treat this as a fixed contract.

## Intent

- Build one aggressive feature branch that boots the rust-owned Pixel path into the real Shadow shell/app experience.
- Optimize for a working demo over small master rungs: compositor shell home, TypeScript app launch, Rust app path, and usable input/control.
- Keep the existing recovered proof discipline so failures identify the next blocker instead of becoming guesswork.

## Scope

- In scope:
  - rust boot seam to `shadow-session`
  - real shell startup config, not dummy shell client
  - GPU shell frame, TypeScript counter/camera/timeline/podcast/cashu launch, Rust app regression path
  - Podcast audio bridge packaging for the rust-booted TypeScript shell path
  - session lifetime/control and touch/manual input when it unblocks the demo
  - payload staging fixes when ramdisk size becomes the blocker
- Out of scope for this branch unless they block the shell/app loop:
  - native audio daemon/mixing beyond the current Linux bridge proof
  - real camera HAL fidelity beyond the mock-allowed camera app launch
  - direct `std` PID1
  - stock-init/imported-rc fallback paths

## Approach

- Continue from current `master` on branch `boot-full-shadow-demo` in `worker-1`.
- Reuse the proven app-direct-present bundle machinery, but add a product shell-session boot mode.
- Prefer `counter` for the first shell-launched TypeScript app because it avoids network/service dependencies.
- Preserve the metadata/recover-traces truth surface and add shell-specific readiness fields.
- Treat the landed `/metadata/shadow-payload/by-token/<run_token>` probe as a small control/proof surface only; it is not large enough for product shell/session payloads.
- Do not prototype runtime payload delivery on `/metadata`; it is about 10 MB and only useful for manifests, breadcrumbs, and recovered proof.
- Consume worker-2's larger payload partition once it lands; until then keep behavior work on the current ramdisk path and keep payload-size-sensitive changes isolated.

## Steps

- [x] Define boot-owned shell startup artifact and staging shape.
- [x] Add shell-session boot mode and host smoke coverage.
- [x] Build and run first shell-home frame proof on a Pixel.
- [x] Add shell-start-app proof for TypeScript `counter`.
- [x] Fold in synthetic runtime touch proof for shell-launched TypeScript `counter`.
- [x] Keep the current ramdisk shell-session bundle working while worker-2 brings up the larger payload partition.
- [x] Move the shell-session runtime bundle onto the larger payload partition once that lands; do not use `/metadata` as the intermediate payload store.
- [x] Add persistent/held shell mode with a clear recovery path.
- [x] Add broader app coverage from the shell: TypeScript `timeline` and Rust `rust-demo` both have hardware proof from the shell path.
- [ ] Fold in manual/real touch plumbing from `worker-2` if it helps interaction.
- [x] Confirm the larger-partition-backed shell/app path on hardware and record proof artifacts.
- [x] Add a one-command full-demo runner for the GPU compositor, TypeScript `counter`/`timeline`, Rust `rust-demo`, sunfish touch bootstrap, and logical-payload partition.
- [x] Expand the full-demo runner/profile to the current app matrix: `counter`, `camera`, `timeline`, `podcast`, `cashu`, `rust-demo`, and `rust-timeline`.
- [x] Include the shared Linux audio bridge and Podcast assets in the rust-booted TypeScript shell path.
- [x] Add host smoke coverage for Rust-primary shell startup with TypeScript extra-app launchers so app-client env cannot poison the primary Rust client.
- [ ] Make the recovered compositor frame match the intended visible app UX; current proof is functionally valid but the captured frame is still shell/chrome dominated.

## Implementation Notes

- `worker-2` has useful manual touch work: `/dev/input/event2` bootstrap and non-synthetic touch config.
- The branch should not add more isolated app-direct rungs unless they directly unblock shell/app launch.
- Static musl `shadow-compositor-guest` could not load the Vulkan/ICD stack on device; shell-session now stages the dynamic `aarch64-linux-gnu` compositor and launches it through the staged loader.
- Build/reproduce the ARM GNU artifacts through the current linux-builder path (`PIXEL_GUEST_BUILD_SYSTEM=aarch64-linux` / `aarch64-linux` flake packages).
- First dynamic shell run proved a GPU shell/home frame, but app-frame exit initially stopped on the home frame. The compositor now defers `exitOnFirstFrame` until the shell-started app has produced the presented frame.
- `/metadata` filled up with old boot-token artifacts and Android root could not delete the boot-created unlabeled directories under SELinux. The boot image builder now has an explicit lab option, `--orange-gpu-metadata-prune-token-root true`, so owned PID1 can reclaim the proof area before writing a fresh token.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-appframe-metrics-r1-20260424014239/device-run/recover-traces-rerun/status.json` proved the shell log path for `counter`; recovery now also requires the captured frame to match the app-specific frame fingerprint before `proof_ok=true`.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-touch-counter-r4-20260424033300/device-run/recover-traces/status.json` proved `shell-session-runtime-touch-counter` with GPU shell, shell-launched TypeScript `counter`, synthetic compositor tap gated on the counter app frame, runtime counter increment, successful post-touch present, and a recovered post-touch shell frame fingerprint. Physical-touch hardware proof is still pending.
- `payload-partition-first-probe` landed on master as a metadata-backed manifest probe, but `/metadata` is only about 10 MB. Use it for breadcrumbs/manifests/proof, not for the real runtime/compositor/app bundle. Worker-2 owns the larger new-partition lane; consume that when available.
- `shell-session` can now stage and launch Rust `rust-demo` through the shell path. The touch-counter shell proof stays TypeScript-only because its evidence contract is tied to hosted runtime counter events.
- `shell-session` now has host/recovery proof coverage for non-counter TypeScript `timeline` as well as `counter`, so the shell app path is no longer counter-only at the script/proof-contract layer.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-held-r1-20260424042557/device-run/recover-traces/status.json` proved `shell-session-held` with GPU shell, shell-launched TypeScript `counter`, durable app-frame metadata, `probe_report_proves_child_timeout=true`, `probe_summary_proves_shell_session_held=true`, and `metadata_compositor_frame_proves_shell_session_app=true`. The generic one-shot helper collection timed out, but recovered metadata proof was `proof_ok=true` and the device returned to rooted Android.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-timeline-r3-20260424050459/device-run/recover-traces-rerun/status.json` proved `shell-session` with GPU shell, shell-launched TypeScript `timeline`, GPU TypeScript renderer, durable app-frame metadata, shell session summary, and app-specific frame colors. The timeline app now includes simple hex background fallbacks so the boot renderer does not fall through transparent when it ignores gradient backgrounds.
- Earlier Rust finding on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-rust-demo-r1-20260424043704/device-run/recover-traces/status.json` launched Rust `rust-demo`, mapped and tracked the surface, and observed a committed app frame, but strict GPU-resident mode rejected the SHM app composition path at `VelloImageRenderer::render_to_vec`.
- The Rust app strict-GPU blocker is now cleared for the shell path: SHM app frames are imported as GPU image brushes and composited into the scanout dmabuf by Vello, so the shell no longer falls back to CPU readback/composition for Rust `rust-demo`.
- The shell/session builder now supports `--orange-gpu-bundle-archive-source shadow-logical-partition`: it keeps the Rust PID1 shim/config/firmware in the boot ramdisk, writes a sibling `orange-gpu.tar.xz` for host staging, points `orange_gpu_bundle_archive_path` at `/shadow-payload/extra-payloads/orange-gpu.tar.xz`, and Rust PID1 mounts the logical payload partition before expanding the compositor/runtime/app bundle into `/orange-gpu`.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-timeline-logical-r1-20260424052405/device-run/recover-traces/status.json` proved `shell-session` with GPU shell, shell-launched TypeScript `timeline`, GPU TypeScript renderer, and the compositor/runtime/app archive staged from `shadow_payload_a` instead of the ramdisk. Recovered proof was `proof_ok=true`, `probe_summary_proves_shell_session=true`, `metadata_compositor_frame_proves_shell_session_app=true`, and the archive staging output recorded `Payload source: shadow-logical-partition`, `Remote payload root: /shadow-payload`.
- Staging note: `scripts/pixel/pixel_boot_stage_metadata_payload.sh` now preserves its original argv across the host-lock re-exec. Without that, parsed `--source shadow-logical-partition` / `--extra-payload ...` options silently fell back to metadata defaults after taking the lock.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-rust-demo-logical-r2-20260424055521/device-run/recover-traces-after-proof-rule/status.json` proved `shell-session` with GPU shell, shell-launched Rust `rust-demo`, and the compositor/runtime/app archive staged from `shadow_payload_a`. Recovered proof was `proof_ok=true`, `probe_summary_proves_shell_session=true`, `metadata_probe_summary_shell_session_mapped_window=true`, `metadata_probe_summary_shell_session_app_frame_captured=true`, and `metadata_compositor_frame_proves_shell_session_app=true`. The report includes `shadow-rust-demo: frame_committed`, `buffer-observed type=shm`, `app_composite_ms=0`, and `presented-shell-dmabuf`; exact app-direct color samples are intentionally not required for this Rust shell-session proof because GPU image filtering can shift sampled colors.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-held-rust-logical-r1-20260424061149/device-run/recover-traces/status.json` proved the same logical-payload Rust shell app path in `shell-session-held` mode. The session stayed in boot-owned userspace until the 120s watchdog returned through fastboot after about 139s, then recovered `proof_ok=true`, `probe_summary_proves_shell_session_held=true`, `probe_report_proves_child_timeout=true`, `metadata_probe_timeout_class_bucket=generic-watchdog`, and `metadata_probe_stage_value=orange-gpu-payload:shell-session-held-watchdog-proved`.
- `--orange-gpu-timeout-action hold` is now supported for Rust PID1 held shell sessions. On watchdog proof it records the same held-session metadata but leaves the compositor/app child running for the configured `--hold-secs` observation window before the normal recovery reboot.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-held-rust-hold-logical-r1-20260424062746/device-run/recover-traces/status.json` proved the logical-payload Rust shell app with a 60s watchdog plus 60s live observation window. The device returned through fastboot after 138s, recovered `proof_ok=true`, `probe_summary_proves_shell_session_held=true`, `probe_report_proves_child_timeout=true`, and `metadata_probe_stage_value=orange-gpu-payload:shell-session-held-watchdog-proved`; recovered logs show `shadow-rust-demo: frame_committed`, `buffer-observed type=shm`, `app_composite_ms=0`, and `presented-shell-dmabuf`.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-touch-counter-logical-r1-20260424063926/device-run/recover-traces/status.json` proved logical-payload TypeScript interaction in `shell-session-runtime-touch-counter`: shell-launched `counter`, GPU TypeScript renderer, synthetic compositor touch dispatched, counter incremented, post-touch frame committed and captured, touch latency present, `metadata_compositor_frame_proves_shell_session_app=true`, and `proof_ok=true`.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-touch-input-logical-r1-20260424064945/device-run/recover-traces/status.json` repeated the logical-payload TypeScript interaction proof with sunfish touch bootstrapped from worker-2's touch modules and combined GPU/touch firmware. Rust PID1 created `/dev/input/event2`; the compositor logged `touch-ready device=/dev/input/event2 name=fts range=0..=1079x0..=2339`; recovered proof remained `proof_ok=true` with counter increment, post-touch frame capture, touch latency, and GPU dmabuf presentation.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-shell-session/ss-held-ts-input-logical-r2-20260424152014/device-run/recover-traces-rerun/status.json` combined the current demo path: logical payload partition, GPU shell compositor, shell-launched TypeScript `counter`, sunfish input bootstrap, synthetic tap after app frame, counter increment, post-touch frame capture, and `--orange-gpu-timeout-action hold` for a 60s live observation window after watchdog proof. Recovered proof is `proof_ok=true`, `probe_summary_proves_shell_session_held=true`, `probe_summary_proves_shell_session_held_touch_counter=true`, `metadata_compositor_frame_proves_app_direct_present=true`, and `metadata_probe_stage_value=orange-gpu-payload:shell-session-held-watchdog-proved`. The first recovery parse of this run used the stale pre-touch counter frame palette and reported only `metadata_compositor_frame_proves_app_direct_present=false`; the recovered traces already contained the post-touch runtime counter frame, and the recovery rule now expects that palette for held counter sessions.
- `scripts/pixel/pixel_boot_full_shadow_demo.sh` is the current end-to-end test runner for the aggressive branch. It builds the rust-booted GPU shell image, stages the compositor/runtime/app archive through the 256 MiB `shadow_payload_a` logical partition, starts `counter`, bundles `camera`, `timeline`, `podcast`, `cashu`, `rust-demo`, and `rust-timeline` for shell launch, enables sunfish DRI/touch bootstrap, and recovers the proof summary/frame.
- The full-demo runner auto-enables `PIXEL_ORANGE_GPU_ENABLE_LINUX_AUDIO=true` when Podcast is included and the start app is TypeScript. Rust start apps still keep audio disabled because the boot packager currently supports Linux audio bridge env/session wiring only through the TypeScript primary runtime path.
- The full-demo runner owns the top-level device lease and sets `SHADOW_DEVICE_LEASE_FORCE=1` only for its internal stage/boot helper calls. Without that handoff, the lower-level helpers reject the wrapper's own active lease before staging the logical payload.
- For manual demo runs, the wrapper exits successfully when recovered metadata proves a held shell session with a captured app frame, even if the stricter `proof_ok` classifier is false. The raw proof fields are still printed so CI-style regressions remain visible.
- Real all-apps audio build artifact: `build/pixel/runs/boot-full-shadow-demo/all-apps-audio-build-r3-20260424T230353Z/` produced a 32 MiB boot image and 43 MiB logical-payload archive containing all TypeScript bundles, Podcast assets, both Rust binaries/launchers, `shadow-audio-bridge`, ALSA config, and `services.audioBackend=linux_bridge` in `shell-session-startup.json`.
- Hardware run `build/pixel/runs/boot-full-shadow-demo/all-apps-audio-device-r1-20260424T232306Z/` built and staged the all-apps audio payload to `shadow_payload_a`, then fastboot-booted `06241JEC200520`. Host proof timed out at `wait-adb` with no recovered metadata; the device later rested in fastboot, was rebooted to Android, and the stale lease was released. Do not count this as a durable hardware audio proof without a physical UI/audio observation or a follow-up observable run.
- Hardware final-default run on `06241JEC200520`: `build/pixel/runs/boot-full-shadow-demo/default-shadow-demo-final-20260425T033923Z/device-run/recover-traces/status.json` built and staged the current default all-app image through `shadow_payload_a` with audio enabled. The 43 MiB payload contained `counter`, `camera`, `timeline`, `podcast`, `cashu`, `rust-demo`, and `rust-timeline`; recovered metadata proved `probe_summary_proves_shell_session_held=true`, `metadata_probe_summary_shell_session_app_frame_captured=true`, `metadata_probe_summary_touch_counter_counter_incremented=true`, and `metadata_probe_summary_touch_counter_touch_latency_present=true`. The stricter proof remained `proof_ok=false` because the synthetic post-touch frame was not captured, but the recovered frame shows the shell chrome plus the visible Counter app surface.
- Hardware proof on `06241JEC200520`: `build/pixel/runs/boot-full-shadow-demo/full-shadow-visible-r2-20260424T165453Z/device-run/recover-traces/status.json` proved the full-demo runner with logical payload partition, GPU shell compositor, TypeScript `counter` start app, extra TypeScript `timeline` bundle, Rust `rust-demo` bundle, sunfish input bootstrap, synthetic tap, counter increment, touch latency, and held-session watchdog proof. Recovered proof was `proof_ok=true`, `probe_summary_proves_shell_session_held=true`, `probe_summary_proves_shell_session_held_touch_counter=true`, `metadata_compositor_frame_proves_app_direct_present=true`, and `metadata_probe_stage_value=orange-gpu-payload:shell-session-held-watchdog-proved`.
- Visual caveat from `full-shadow-visible-r2/r3/r4`: the recovered `metadata-compositor-frame.ppm` is still dominated by shell chrome/dark shell content even after simplifying `counter` to brighter root/block backgrounds. The payload archive contains the updated bright bundle and functional metadata proof passes, but this recovered frame is not yet a trustworthy UX screenshot. Manual device testing should check whether live app launch is brighter than the recovered proof frame; if not, the next task is to trace why the shell capture path is preserving the dark shell/home frame instead of the current hosted app surface.
- `rust-demo` extra-app staging had a real packaging gap: the archive contained `shadow-rust-demo` but startup config pointed `SHADOW_APP_CLIENT_RUST_DEMO` at missing `run-shadow-rust-demo`. The shell-session builder now renders a per-Rust-app launcher and asserts all startup app-client paths exist and are executable before archiving.
- Sidecar hardware proof on `09051JEC202061`: `build/pixel/runs/boot-app-direct-counter-visual/app-direct-counter-visual-r1-20260424T180115Z/device-run/recover-traces/status.json` showed app-direct TypeScript `counter` is bright on hardware with the GPU renderer. Runtime touch/counter proof passed (`counter_incremented=true`, `post_touch_frame_captured=true`, `touch_latency_present=true`), and the recovered frame was 99% bright pixels. The automated app-direct color predicate still returned false, but the frame itself rules out a broken counter bundle or fundamental TypeScript renderer dark-screen issue.
- Historical dark-screen investigation: before the font fix, hosted TypeScript composition was the leading suspect because TypeScript apps in shell mode paint directly into the shell scanout. The `hosted-pre-compose-summary` and `hosted-post-compose-summary` diagnostics are now gated behind `SHADOW_GUEST_COMPOSITOR_GPU_PROFILE_TRACE`; keep them for future hosted-composition investigations, but the current visible UX issue was the missing curated font payload.
- Manual test on `boot-shell-guard-r1-20260424T195236Z` improved the visible UX: `counter` and `rust-demo` behaved correctly from the rust-booted shell, while `timeline` opened but rendered no text. Recovered logs showed the matching old rooted-Pixel failure mode: `[shadow-blitz-demo] system-font-context-unavailable reason=curated-android-fonts-missing`.
- The rooted-Pixel fix was `ba2da35` + `0d86bc1`: Blitz learned curated Android font loading, and the Pixel bundle staged reproducible Roboto/Roboto Mono/Noto emoji files as `system/fonts/*`. The rust-boot shell now stages the same curated fonts into the logical payload archive under `/orange-gpu/system/fonts`; `shadow-blitz-demo` probes that payload path before falling back to `/system/fonts`, so Timeline text should not depend on a chrooted rooted-Pixel launcher.
- Manual test on `boot-shell-fonts-r1-20260424T201113Z` confirmed the current visible UX: `counter`, `timeline`, and `rust-demo` all open and behave from the rust-booted shell. Timeline text renders again; the app has no real events until the Wi-Fi/network lane lands, which is expected. The recovered log includes `[shadow-blitz-demo] registered-curated-android-fonts count=4 elapsed_ms=0`, proving the boot payload font fix is active. The durable shell proof still reports `proof_ok=false` only because the held-session synthetic counter contract did not capture a post-touch frame in this manually exercised run (`counter_incremented=true`, `touch_latency_present=true`, `post_touch_frame_captured=false`); keep that proof-rule cleanup separate from the visible UX fix.
- App matrix hardware status after the profile expansion: `camera`, `timeline`, `podcast`, and `cashu` each proved `proof_ok=true` as TypeScript start apps; `rust-demo` proved shell launch and app frame capture with the Rust client; `rust-timeline` now proves `proof_ok=true` after vendoring `masonry_winit` with a no-op clipboard fallback for direct-FD Wayland sessions without X11. The old failure was a panic from `copypasta` selecting X11 and unwrapping `XcbConnect(DisplayNotSet)`.
- Rust UI font follow-up: the Rust/Xilem SDK now mirrors the rooted-Pixel TypeScript font fix by loading curated Android fonts from `SHADOW_RUST_UI_FONT_DIR`, `SHADOW_BLITZ_FONT_DIR`, `/orange-gpu/system/fonts`, or `/system/fonts`, then applying the `Roboto, 'Droid Sans', 'Noto Sans', 'Noto Color Emoji', sans-serif` stack to SDK labels, prose, and text inputs. The boot bundle stages curated fonts for Rust clients too, so `rust-timeline` should no longer depend on host/system font discovery in the rust-boot shell.
- Hardware proof on `09051JEC202061`: `build/pixel/runs/boot-full-shadow-demo/rust-timeline-loader-path-20260425T181353Z/device-run/recover-traces/status.json` proved `rust-timeline` as the start app in the full rust-boot shell demo. Recovered proof was `proof_ok=true`, `probe_summary_proves_shell_session_held=true`, `metadata_compositor_frame_proves_app_direct_present=true`, and `metadata_probe_stage_value=orange-gpu-payload:shell-session-held-watchdog-proved`. The recovered frame shows the Rust Nostr account setup screen with readable text.
- Rust Timeline WGPU root cause: the Rust app launcher was passing only `/orange-gpu/app-direct-present/lib` to the staged dynamic loader. That let the app create a Wayland surface but hid the root `/orange-gpu/lib` Vulkan stack from `dlopen`, so Masonry/Xilem failed with `WgpuCreateSurfaceError(CreateSurfaceError { inner: Hal(FailedToCreateSurfaceForAnyBackend({})) })`. The launcher now keeps both app-local libs and `/orange-gpu/lib` in the loader search path; recovered logs from the proof run show `shadow-masonry-wgpu: selected-adapter backend=Vulkan ... name="Turnip Adreno (TM) 618"`.
- Review cleanup: the TypeScript Camera app no longer carries global `SHADOW_RUNTIME_CAMERA_ALLOW_MOCK` launch env in `runtime/apps.json`. The boot demo instead scopes mock camera availability through the boot session config's `services.camera.allowMock=true`, and Rust primary app launches now receive `SHADOW_RUNTIME_SESSION_CONFIG` so `rust-timeline` can use the shared Nostr service paths rather than only drawing a first frame.
- Final hardware proof after review cleanup on `09051JEC202061`: `build/pixel/runs/boot-full-shadow-demo/rust-timeline-final-config-20260425T194619Z/device-run/recover-traces/status.json` proved `rust-timeline` as the full shell start app with `proof_ok=true`, `probe_summary_proves_shell_session_held=true`, `metadata_compositor_frame_proves_app_direct_present=true`, and `metadata_probe_stage_value=orange-gpu-payload:shell-session-held-watchdog-proved`. The staged `shell-session-startup.json` exports `SHADOW_RUNTIME_SESSION_CONFIG=/orange-gpu/shell-session-startup.json`, carries Nostr DB/socket plus Cashu data-dir services, and scopes the boot-only mock camera through `services.camera.allowMock=true`. Recovered logs show the app launcher loader path as `/orange-gpu/app-direct-present/lib:/orange-gpu/lib`, Rust UI registering 4 curated fonts, and Masonry selecting the Turnip Vulkan adapter.
