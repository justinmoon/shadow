#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=./pixel_common.sh
source "$SCRIPT_DIR/pixel_common.sh"
ensure_bootimg_shell "$@"

target="${PIXEL_SERIAL:-pixel}"
exec "$SCRIPT_DIR/shadowctl" --target "$target" "$@"
