[package]
name = "init-wrapper"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
lzma-rs = "0.3"
sha2 = "0.10"
tar = { version = "0.4", default-features = false }

[workspace]

[profile.release]
panic = "abort"
