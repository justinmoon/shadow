[package]
name = "runtime-nostr-host"
version = "0.1.0"
edition = "2021"

[dependencies]
deno_core = { version = "=0.393.0", default-features = false, features = ["include_icu_data"] }
deno_error = "=0.7.1"
rusqlite = "0.37.0"
serde = { version = "1.0.228", features = ["derive"] }
